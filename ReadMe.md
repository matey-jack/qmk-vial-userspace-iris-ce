
This repo only exists to run a GitHub Actions flow for building QMK-VIAL, cloned from `git@github.com:vial-kb/vial-qmk.git`.

The keyboard is set to keebio/iris_ce.

We'll see if we need any other settings.

